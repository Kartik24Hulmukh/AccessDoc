Prepare AccessDoc 0.4.0-beta.4 for GitHub PR and a protected Vercel preview. Use only `repository/` as the GitHub root. Show the exact changed-file list and SHA. Run all repository tests and release verification; stop on failure. Do not publish or deploy production automatically. After CI passes, import the repo into Vercel with framework Other, no build command, no output directory, and Python 3.13 from `.python-version`. Do not add secrets. Run sanitized preview checks for root/static/sample routes, `POST /api/bundle`, ZIP manifest hashes, PDF/HTML/receipt consistency, malformed/oversized/cross-site failures, cache/security headers, cold/warm latency, mobile/keyboard/browser behavior, and privacy-safe logs. Return GO/NO-GO, confidence, weakest assumption, falsifier, kill criteria, commit SHA, deployment ID/URL, and rollback target. Keep Vercel-package readiness separate from hosted-preview and production evidence.
